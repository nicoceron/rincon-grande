---
interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
const isExternal = /^(https?:)?\/\//.test(href);
---

<a
  class="source-route-button"
  href={href}
  target={isExternal ? '_blank' : undefined}
  rel={isExternal ? 'noopener' : undefined}
>{label}</a>
